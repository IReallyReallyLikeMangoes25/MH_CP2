# MH 1st run project functions

# run update personal library:
    # opens terminal widget for personal library and runs it

# run fractal generator:
    # opens terminal widget for fractal generator and runs it

# run simple gradebook:
    # opens terminal widget for simple gradebook and runs it

# run pet simulator:
    # opens terminal widget for petsimulator and runs it
